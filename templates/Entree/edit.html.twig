{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' "RSSuiviDeProjetBundle:Entree:form.html.twig" %}
{% extends "base.html.twig" %}

{% block title %}
    {{ 'entree.editer'|trans() }}
{% endblock title %}

{% block body %}
    <div class="container-fluid">
    <p class="lead">{{ 'entree.editer'|trans() }}</p>
    <div class="row">
        <div class="col-lg-6">
        {{ form(form) }}
        </div>
    </div>
</div>
{% endblock body %}
