{% form_theme form 'form.html.twig' %}
{% extends "base.html.twig" %}

{% block title %}
    {{ 'image.editer'|trans() }}
{% endblock title %}

{% block body %}
    <div class="container-fluid">
    <p class="lead">{{ 'image.editer'|trans() }}</p>
    <div class="row">
        <div class="col-lg-6">
        {{ form(form) }}
        </div>
    </div>
</div>
{% endblock body %}
