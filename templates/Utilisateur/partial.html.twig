{% if users|length > 0 %}
<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{ 'intervenants'|trans }} <span class="caret"></span></a>
    <ul class="dropdown-menu" role="menu">
    {% for user in users %}
        <li><a href="{{ path('rs_suivideprojet_utilisateur_affichermodulesparuser', {'user': user.id}) }}">{{ user.prenom }} {{ user.nom }}</a></li>
    {% endfor %}
        <li><a href="{{ path('rs_suivideprojet_modulefonctionnalitetype_listemodules') }}">{{ 'tous'|trans() }}</a></li>
    </ul>
</li>
{% endif %}
